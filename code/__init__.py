"""Top-level package for the Multi-Modal Evidence Review solution.

This package intentionally shadows the stdlib ``code`` module within this
project because the contest contract fixes the entry points under ``code/``
(see AGENTS.md s6). Imports use the fully-qualified ``code.<module>`` form.
"""
